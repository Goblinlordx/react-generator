import {delay} from 'redux-saga';
import {take, put} from 'redux-saga/effects';
import T from 'action/type';
const {LOGIN, LOGOUT, AUTH_START, AUTH_SUCCESS, AUTH_FAIL, FLUSH} = T;

export default function*() {
}
